export const metadata = {
  title: "Keyword scanner",
  description:
    "Compare your resume to a job description with AI-powered ATS keyword matching and suggestions.",
};

export default function KeywordScannerLayout({ children }) {
  return children;
}
