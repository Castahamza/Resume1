export const metadata = {
  title: "Edit resume",
  description: "Edit your resume with live preview and AI-assisted bullet points.",
};

export default function EditResumeLayout({ children }) {
  return children;
}
