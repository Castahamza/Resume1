export const metadata = {
  title: "Cover letters",
  description:
    "Generate tailored cover letters from your resume and job postings, then save or export to PDF.",
};

export default function CoverLetterLayout({ children }) {
  return children;
}
